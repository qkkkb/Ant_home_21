# AGENTS.md

## 生效范围

以下规则仅在 `D:\Ant_Home` 目录及其子目录内生效。

如果当前任务涉及的文件不在 `D:\Ant_Home` 下，不要执行本文件中的 Git 自动提交、推送或 README 更新规则。

每次开启新对话时，先读取本文件，再开始分析或修改。

## 协作规则

1. 写代码或改文档前，先列出计划，等待用户确认后再修改。
2. 如果用户的 prompt 有不理解或可能跑偏的地方，先提问，不要猜测执行。
3. 修改时只处理本次任务相关文件，不要顺手改无关文件。
4. 如果工作区已有用户未提交的改动，要保留不动；提交时只暂存本次任务相关文件。

## Git 工作约束

### 自动提交规则

每次完成代码或文档修改后，如果修改内容位于 `D:\Ant_Home` 目录内，必须执行一次 Git 提交。

提交前需要完成以下检查：

1. 确认当前 Git 仓库路径属于 `D:\Ant_Home`。
2. 使用 `git status` 检查变更文件。
3. 只提交与本次任务相关的文件，不要提交无关文件、临时文件、缓存文件或用户未要求修改的文件。
4. 优先使用精确路径暂存文件，例如 `git add -- AGENTS.md README.md`，不要默认使用 `git add .`。
5. 如果项目有测试、构建或格式化命令，应优先运行必要检查，确认没有明显错误后再提交。
6. 如果检查命令不存在、无法运行或与本次文档修改无关，可以继续提交，但最终回复中要说明未运行原因。

提交信息要求：

1. 必须使用中文。
2. 提交信息要简洁、明确，能概括本次修改内容。
3. 默认编码方式为 UTF-8。
4. 不要使用英文提交信息。
5. 不要使用无意义提交信息，例如 `update`、`fix`、`修改`、`提交代码`。

推荐格式：

```txt
优化：完善用户登录逻辑
修复：解决页面刷新后状态丢失问题
新增：添加订单列表筛选功能
调整：更新接口请求参数
文档：更新 README 使用说明
```

### 推送规则

1. 除非用户明确要求，不要自动执行 `git push`。
2. 每次推送前，必须检查根目录 `README.md` 是否需要同步更新。
3. 如果本次提交影响项目说明、目录结构、运行方式、依赖、配置参数、硬件连接、标定流程或重要行为，先更新 `README.md` 并提交，再执行推送。
4. 如果确认 `README.md` 无需更新，最终回复中要说明已检查且无需更新。
5. 当未推送提交超过 10 条时，只提醒用户是否需要推送，不要自动推送。
6. 不要自动执行 `git push --force` 或 `git push --force-with-lease`；除非用户明确要求并确认风险。

### 历史与风险操作

除非用户明确要求并确认风险，不要执行以下操作：

1. `git rebase`
2. `git reset`
3. `git commit --amend`
4. `git cherry-pick`
5. `git revert`
6. 改写历史或强制推送相关操作

如果当前仓库处于 merge、rebase、cherry-pick、冲突状态或 detached HEAD，先停止并询问用户，不要强行提交。

## 文档放置规则

1. `AGENTS.md` 放在仓库根目录，作为工具协作规则入口。
2. `README.md` 放在仓库根目录，作为 GitHub 仓库首页说明。
3. 扩展文档放在 `docs/` 目录，例如硬件说明、标定记录、调试笔记等。
4. 如果代码行为、运行方式、目录结构或硬件依赖发生变化，同步更新相关文档。

## MicroPython 内存优化

生成 MicroPython 代码时，请优先按内存受限设备优化：

1. 避免在模块 import 阶段创建大对象、初始化硬件或启动长期任务；所有 import 完成后再显式 init。
2. 对稳定模块优先考虑 `.mpy` 预编译或 frozen bytecode，常量数据尽量放在 frozen 模块中。
3. 常量使用 `micropython.const()`；仅模块内部使用的常量名前加下划线，例如 `_BUF_SIZE = const(64)`。
4. 热循环、控制循环、IO 循环中不要反复创建对象；缓冲区、数组、状态对象要在初始化阶段预分配并复用。
5. IO 读取优先使用 `readinto(buf)`，不要在循环里反复 `read()` 产生新 `bytes`。
6. 数据存储优先使用 `bytes`、`bytearray`、`array`、`struct`，而不是 `list[int]`；二进制数据不要用 `str`。
7. 避免 `list` 反复 `append` 导致扩容；能固定长度就预分配。
8. 避免运行期字符串拼接；常量字符串用相邻字面量，动态输出尽量分段写入。
9. 避免大切片复制；需要传递缓冲区片段时优先用 `memoryview`，但不要让 `memoryview` 长期持有大缓冲区。
10. 热路径尽量使用整数或定点数，避免 `float`，特别是在无 FPU 或 `float` 会上堆的端口。
11. 禁用 `eval` 和 `exec`；序列化优先考虑 `json`，避免依赖会触发 `exec` 的方案。
12. 大的长期缓冲区应尽早创建，降低堆碎片风险；运行中定期在可接受的时机 `gc.collect()`。
13. 需要观测内存时使用 `gc.mem_free()`、`gc.mem_alloc()`、`micropython.mem_info()`。
14. 不要为了速度盲目加 `@micropython.native` 或 `@micropython.viper`；`native` 会增大代码体积，`viper` 也有语义限制，只在测量后用于热点。

## GC 控制约束

1. MicroPython 使用 Mark and Sweep GC；不要依赖“内存快满时自动 GC”，应在可接受暂停的位置主动安排 `gc.collect()`。
2. 在进入时间敏感控制段、传感器采样段、电机控制段、通信时序段之前，先执行 `gc.collect()`，避免关键路径中突然触发 GC。
3. 在创建并释放大对象后，应显式 `del` 对象引用，然后在空闲点 `gc.collect()`。
4. 程序初始化完成后，可设置 `gc.threshold(gc.mem_free() // 4 + gc.mem_alloc())`，让 GC 更早触发，减少堆碎片风险。
5. 不要在硬实时或高频循环里执行可能分配内存的操作；如需验证关键段零堆分配，可用 `micropython.heap_lock()` 和 `micropython.heap_unlock()` 包住该段。
6. 使用 `heap_lock()` 时必须用 `try/finally` 确保解锁；锁堆期间禁止创建新对象、字符串格式化、切片复制、`list` 或 `dict` 扩容、`import`、异常构造、`print` 调试等可能分配堆内存的操作。
7. `gc.disable()` 只是不自动 GC，不代表禁止分配；若目标是验证“无堆分配”，应使用 `heap_lock()`。
8. `heap_lock()` 属于严苛场景工具，不作为普通优化手段；普通代码优先通过预分配 `buffer`、复用对象、减少临时对象来降低 GC 压力。

## MicroPython 项目分文件约束：

1. 不要把代码拆成大量很小的 .py 文件；普通文件系统上的 .py 在 import 时会被编译成 bytecode，并占用 RAM。
2. 每个模块 import 时只能定义 const、类、函数、少量不可变常量；禁止在模块顶层初始化硬件、创建大对象、创建 buffer、启动循环或执行业务逻辑。
3. 所有模块先 import 完，再由 main.py 统一调用 init()/setup() 创建运行期对象，这样能给编译器留下最大 RAM。
4. 可复用且稳定的驱动、算法、查表数据、协议解析模块，优先独立成文件，方便之后编译成 .mpy 或 frozen bytecode。
5. 大型只读数据应单独放到常量模块里，用 bytes、tuple、str 等不可变对象表示；如果固化到 firmware，可尽量留在 flash。
6. 可变运行状态、buffer、队列、传感器对象、电机对象不要放在常量模块顶层，应在 init 阶段集中创建。
7. 避免循环 import；模块依赖方向保持简单：main -> app/control -> drivers/utils/config。
8. 高频控制循环相关代码应少跨模块调用、少动态查找；必要时把热路径函数集中在少数模块中。
9. boot.py 保持极简，只做必须的启动配置；main.py 负责 import、gc.collect()、gc.threshold()、初始化、进入主循环。但目前该项目已经不需要boot.py文件了，可直接main.py启动
